import { createSlice } from '@reduxjs/toolkit';

export const userBuddiesSlice = createSlice({
  name: '',
  initialState: {
    value: []
  },
  reducers: {}
});

export const {} = userBuddiesSlice.actions;

export default userBuddiesSlice.reducer;